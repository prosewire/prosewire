---
"@prosewire/sdk": patch
"@prosewire/cli": patch
"@prosewire/mcp": patch
---

Make publication selection explicit across public clients and CLI usage, safely encode publication paths, and expose the API-key-scoped publication to MCP clients.
